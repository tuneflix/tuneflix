import routes from './routes'

// jest.mock('react-router-dom', () => ({
//     Link: 'Link',
//     Route: ({children, path}) => children({match: path === ''})
// })
// )
test('Dummy test while researching', () => {
    const num1 = 1;
    const num2 = 2;
    expect (num1 + num2).toEqual(3)
})




